name: ${project.name}
version: ${project.version}+${project.build.number}.${git.commit.id.abbrev}
description: ${project.description}
author: ${bukkit.plugin.authors}
website: ${project.url}
main: ${bukkit.plugin.main}
